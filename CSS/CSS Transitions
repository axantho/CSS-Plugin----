/* Smooth hover transitions */
.card {
  width: 300px;
  height: 200px;
  background: #007acc;
  transition: transform 0.3s ease, background 0.3s ease;
}

.card:hover {
  transform: scale(1.1);
  background: #005f73;
}
