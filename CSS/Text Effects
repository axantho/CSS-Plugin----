/* Gradient text */
.gradient-text {
  font-size: 48px;
  font-weight: bold;
  background: linear-gradient(45deg, #ff6f61, #ffaf00);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
